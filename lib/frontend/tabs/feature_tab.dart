import 'package:flutter/material.dart';

class FeatureTab extends StatelessWidget {
  const FeatureTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Feature Screen", style: TextStyle(fontSize: 24)),
    );
  }
}
