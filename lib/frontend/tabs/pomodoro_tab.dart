import 'package:flutter/material.dart';

class PomodoroTab extends StatelessWidget {
  const PomodoroTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Pomodoro Screen", style: TextStyle(fontSize: 24)),
    );
  }
}
