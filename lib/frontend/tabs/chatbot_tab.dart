import 'package:flutter/material.dart';

class ChatbotTab extends StatelessWidget {
  const ChatbotTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("AI Chatbot Screen", style: TextStyle(fontSize: 24)),
    );
  }
}
